Excellent, but before you move to the next stag, there are some mistakes I want you to fix: (1- First, in the university interfaces in "university-dialog.tsx", after I enter the data into the form and hit submit, the form fields are only cleared, and the form returns empty without adding the data to the database. Also, in updating the university, the data is not updated, but when I click the update button, the form returns the data before the update. Additionally, when deleting, after clicking the delete button, the selected item is not removed.) 2- (As for majors, when I open the interfaces at http://localhost:3000/admin/majors, this error appears upon opening: "Error: PrismaClient is unable to run in this browser environment, or has been bundled for the browser (running in ``). If this is unexpected, please open an issue: https://pris.ly/prisma-prisma-bug-report srcservicesuniversity.service.ts (5:23) @ getUniversities 3 | 4 | export async function getUniversities() { > 5 | return await prisma.university.findMany({ | ^ 6 | orderBy: { 7 | createdAt: "desc", 8 | },"

This is part of the issue. When clicking on 'Add MajorDialog', an empty SelectItem is displayed. When refreshing, it also appears empty. Additionally, when clicking on delete, the selected row does not get deleted. Also, when opening 'http://localhost:3000/admin/subjects', this error appears at the bottom of the screen: 'Error: PrismaClient is unable to run in this browser environment, or has been bundled for the browser (running in ``). If this is unexpected, please open an issue: https://pris.ly/prisma-prisma-bug-report srcservicesmajor.service.ts (5:23) @ getMajors 3 | 4 | export async function getMajors() { > 5 | return await prisma.major.findMany({ | ^ 6 | orderBy: { 7 | createdAt: "desc", 8 | }.' Also, when clicking on add, the specializations are not shown, and when editing, the same issue occurs. Additionally, deletion does not execute on the selected row. Please fully fix these errors and ensure the work is professional. Then continue working on 'chapters' before we move on to the next stage.